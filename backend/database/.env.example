# Copy this file to .env and adjust if needed. Defaults below match
# docker-compose.yml exactly, so local dev works with zero changes.
DB_USER=ai_job_agent
DB_PASSWORD=dev_password_change_me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ai_job_agent
